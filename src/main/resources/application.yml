spring:
  flyway:
    baselineOnMigrate: true

  datasource:
    driverClassName: org.h2.Driver
    url: "jdbc:h2:mem:testdb"
    username: sa
    password: password

  security:
    user:
      name: admin
      password: password
